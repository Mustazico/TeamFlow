# Screenshots

Drop PNGs here with these filenames so the README renders them:

- `dashboard.png` — Dashboard page with stat cards and charts
- `kanban.png` — Project Kanban board with dnd
- `task-detail.png` — Task detail drawer with comments + mentions
- `dark-mode.png` — Any page in dark mode

Recommended size: ~1600×1000, PNG, < 500 KB each.
